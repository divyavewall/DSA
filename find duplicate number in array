class Solution {
    public int findDuplicate(int[] nums) {
        Arrays.sort(nums);
        int duplicate = 0;
        for(int i =0; i < nums.length; i++){
            if(i == nums[i]){
                duplicate = nums[i];
                break;
            }
        }
        return duplicate;
    }
}
